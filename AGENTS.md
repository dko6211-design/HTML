# 작업 규칙

## IT 용어 질문 처리

- 사용자가 IT 용어를 물어보면, 설명만 제공하지 말고 반드시 웹 용어 정리집에 새로운 용어집을 추가한다.
- 새 용어는 `html/terms/{용어를 영문 소문자 슬러그로 변환}.html` 파일로 만든다.
- 용어 파일에는 용어명, 분류, 이해하기 쉬운 설명을 작성하고 `../assets/style.css`를 연결한다.
- `html/index.html`의 `terms` 목록에 새 용어의 이름, 분류, 파일 링크를 추가한다.
- 용어 상세 파일의 `← 목록으로 돌아가기` 링크는 반드시 `../index.html`로 연결한다.
- 추가 후 파일 경로와 메인 목록 링크가 정상적으로 연결되는지 확인한다.
